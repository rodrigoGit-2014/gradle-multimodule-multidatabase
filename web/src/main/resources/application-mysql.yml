spring:
  datasource:
    url: jdbc:mysql://localhost:3306/examLabDB
    username: root
    password: mysecretpassword
    #driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
